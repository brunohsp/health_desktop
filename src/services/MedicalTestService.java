package services;

import java.util.ArrayList;
import java.util.List;

import entities.MedicalTest;

public class MedicalTestService {
	public List<MedicalTest> listSpecialties(String name, String code, float value){
		List<MedicalTest> medicalTests = new ArrayList<MedicalTest>();
		
		if (name != "") {}
		if (value != -1.0) {}
		if (code != "") {}
		
		// database code - build search string in ifs
		
		return medicalTests;
	}
	
	public void insert(MedicalTest medicalTest) {
		// ...
	}
}
